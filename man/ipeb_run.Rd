% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{ipeb_run}
\alias{ipeb_run}
\title{Fit and evaluate iPEB in one call}
\usage{
ipeb_run(
  train,
  test,
  markers,
  id = "id",
  case = "case",
  time = "time",
  time_to_dx = "time_to_dx",
  covariates = character(0),
  objective = c("sensitivity", "leadtime", "combined"),
  alpha = 0.95,
  window = Inf,
  slope = c("auto", "on", "off"),
  innovation = c("auto", "ar1", "iid"),
  select = c("none", "backward"),
  n_markers = NULL,
  validation_frac = 0.25,
  specificities = NULL,
  eval_window = NULL
)
}
\arguments{
\item{train}{Training data frame.}

\item{test}{Test data frame (same schema as \code{train}).}

\item{markers}{Character vector of biomarker column names.}

\item{id, case, time, time_to_dx}{Column names for the subject identifier, the
case indicator (1 = case, 0 = control), the visit time, and each visit's
time before diagnosis (in days; used to measure lead time).}

\item{covariates}{Character vector of covariate column names to adjust for
(default none).}

\item{objective}{Clinical objective: \code{"sensitivity"} (default),
\code{"leadtime"}, or \code{"combined"}. Each fixes an internal penalty
profile rather than requiring manual tuning.}

\item{alpha}{Operating specificity at which the combiner is optimized
(default \code{0.95}).}

\item{window}{Optional detection window in months restricting the objective
to a fixed pre-diagnostic horizon. The default, \code{Inf}, uses the whole
pre-diagnostic trajectory (no window).}

\item{slope}{Random-slope layer: \code{"auto"} (default; on when subjects
have enough visits), \code{"on"}, or \code{"off"}.}

\item{innovation}{Residual model: \code{"auto"} (default) or \code{"ar1"}
use gap-scaled AR(1)/OU innovations; \code{"iid"} uses independent
innovations.}

\item{select}{Feature selection by objective-driven backward elimination:
\code{"none"} (default) or \code{"backward"}.}

\item{n_markers}{Optional target panel size when \code{select} is not
\code{"none"}.}

\item{validation_frac}{Fraction of training subjects held out as an internal
validation slice to choose the combiner variant and drive selection
(default \code{0.25}).}

\item{specificities}{Numeric vector of target specificities for evaluation.
Defaults to \code{alpha}.}

\item{eval_window}{Optional evaluation detection window in months (passed to
\code{\link{evaluate}}); \code{NULL} reuses the training window.}
}
\value{
A list with two elements: \code{fit} (the fitted \code{"ipeb"}
  object) and \code{evaluation} (the data frame returned by
  \code{\link{evaluate}} on \code{test}).
}
\description{
Convenience wrapper that fits \code{\link{ipeb}} on \code{train} and
evaluates the fitted model on \code{test}.
}
\examples{
data(ipeb_example)
train <- subset(ipeb_example, split == "train")
test  <- subset(ipeb_example, split == "test")
\donttest{
res <- ipeb_run(train, test, markers = c("m1", "m2", "m3"),
                objective = "sensitivity", innovation = "iid", slope = "off",
                specificities = c(0.90, 0.95, 0.99))
res$evaluation
}

}
\seealso{
\code{\link{ipeb}}, \code{\link{evaluate}}
}
